# Derived layer — regenerable, never canonical.
# Deleting any of this loses nothing but convenience.

data/quiz-log.jsonl
data/*.sqlite
data/*.db
*.csv
dist/
exports/

# Node
node_modules/
npm-debug.log*
.env
.env.local

# Editors / OS
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*~

# Backups — if you need a corpus from last Tuesday, that's what git is for.
data/*.bak
data/*.json.orig
data/traduzioni_v2.json.*

# NOT ignored, deliberately:
#   data/traduzioni_v2.json   the corpus is the whole point of the repo
#   config/prefs.yml          small, personal, and you want it versioned